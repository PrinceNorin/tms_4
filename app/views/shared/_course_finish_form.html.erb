<%= form_for [:supervisor, user.enrolled(course)], data: { remote: true } do |f| %>
  <%= f.submit f.object.status? ? "Force unfinish" : "Force finish",
      class: "btn btn-sm btn-default" %>
<% end %>